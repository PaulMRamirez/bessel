// @bessel/pal-web: the web PAL implementation. Phase 0 implements the HTTP-range
// KernelSource and an OPFS-backed cache; Phase 2 completes offline operation.
export const PAL_WEB_PLACEHOLDER = true;
